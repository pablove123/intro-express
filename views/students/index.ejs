<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todo Index</title>
</head>
<body>
  <h1>Students</h1>
  <ul>
    <% students.forEach(student => { %>
      <li> <%= student.name %>
        -
        <%= student.current ? "Current Student" : "Previous Student" %> 
        -
        ID:<%= student._id %> 
        - 
        Grade: <%= student.grade %> 
        - 
        <%= student.pass ? "Student Passed" : "Student Failed" %> 
     </li>
    <% }) %>
  </ul>
</body>
</html>